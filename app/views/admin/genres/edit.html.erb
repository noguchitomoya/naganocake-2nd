<%= form_with model: @edit_genre, url: admin_genre_path do |f| %>
<%= f.text_field :name %>

<label>
    <%= f.radio_button :is_active, true %> 有効 </label>
<label><%= f.radio_button :is_active,false %>無効</label>
<%= f.submit %>
<% end %>